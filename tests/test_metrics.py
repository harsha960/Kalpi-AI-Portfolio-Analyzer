"""Unit tests for the deterministic metric functions in backend.core.

These lock in the math (Sharpe, volatility, max drawdown, VaR, correlation)
so we can trust the numbers the agent reports. Implemented in a later step.
"""
